pkgdatadir = get_option('prefix') / get_option('datadir') / meson.project_name()
resourcedir = pkgdatadir / 'xournalpp_demo' / 'files'

files = ['sample.pdf', 'test_document.pdf']
install_data(files, install_dir: resourcedir)
